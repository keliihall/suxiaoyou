# 领域文档入口

本仓库使用单一上下文。进行架构设计、问题诊断、测试驱动开发或撰写 issue 前，先阅读以下资料：

- 仓库根目录的 `CONTEXT.md`，了解项目词汇。
- `docs/adr/` 下与任务相关的技术决策记录。
- `.dev/ARCHITECTURE.md`，当任务涉及模块依赖规则时阅读。
- `.dev/CONVENTIONS.md`，当任务涉及编码或测试约定时阅读。

撰写 issue 标题、重构建议、诊断假设和测试名称时，尽量使用 `CONTEXT.md` 中的词汇。

如果新的方案和某条 ADR 冲突，需要明确说明冲突点，不要静默覆盖既有决策。
